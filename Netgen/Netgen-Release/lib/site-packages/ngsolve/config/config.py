def _cmake_to_bool(s):
    return s.upper() not in ['', '0','FALSE','OFF','N','NO','IGNORE','NOTFOUND']

is_python_package    = _cmake_to_bool("")

BUILD_STUB_FILES     = _cmake_to_bool("ON")
BUILD_UMFPACK        = _cmake_to_bool("")
ENABLE_UNIT_TESTS    = _cmake_to_bool("OFF")
INSTALL_DEPENDENCIES = _cmake_to_bool("OFF")
USE_CCACHE           = _cmake_to_bool("OFF")
USE_HYPRE            = _cmake_to_bool("OFF")
USE_LAPACK           = _cmake_to_bool("ON")
USE_MKL              = _cmake_to_bool("ON")
USE_MUMPS            = _cmake_to_bool("OFF")
USE_PARDISO          = _cmake_to_bool("OFF")
USE_UMFPACK          = _cmake_to_bool("OFF")
USE_CUDA             = _cmake_to_bool("OFF")

NETGEN_DIR = "D:/source/cenos/backend/temp/netgen"

NGSOLVE_COMPILE_DEFINITIONS         = "HAVE_NETGEN_SOURCES;USE_TIMEOFDAY;TCL;LAPACK;USE_PARDISO;NGS_PYTHON"
NGSOLVE_COMPILE_DEFINITIONS_PRIVATE = "USE_MKL"
NGSOLVE_COMPILE_INCLUDE_DIRS        = ""
NGSOLVE_COMPILE_OPTIONS             = "/std:c++20;/bigobj;/wd4068;-DMAX_SYS_DIM=3"

NGSOLVE_INSTALL_DIR_PYTHON   = "Lib/site-packages"
NGSOLVE_INSTALL_DIR_BIN      = "bin"
NGSOLVE_INSTALL_DIR_LIB      = "lib"
NGSOLVE_INSTALL_DIR_INCLUDE  = "include"
NGSOLVE_INSTALL_DIR_CMAKE    = "cmake"
NGSOLVE_INSTALL_DIR_RES      = "share"

NGSOLVE_VERSION = "6.2.2606-158-g6417c47b0"
NGSOLVE_VERSION_GIT = "v6.2.2606-158-g6417c47b0"
NGSOLVE_VERSION_PYTHON = "6.2.2606.post158.dev0"

NGSOLVE_VERSION_MAJOR = "6"
NGSOLVE_VERSION_MINOR = "2"
NGSOLVE_VERSION_TWEAK = "158"
NGSOLVE_VERSION_PATCH = "2606"
NGSOLVE_VERSION_HASH = "g6417c47b0"

CMAKE_CXX_COMPILER           = "C:/Program Files/Microsoft Visual Studio/2022/Community/VC/Tools/MSVC/14.41.34120/bin/Hostx64/x64/cl.exe"
CMAKE_CUDA_COMPILER          = ""
CMAKE_C_COMPILER             = "C:/Program Files/Microsoft Visual Studio/2022/Community/VC/Tools/MSVC/14.41.34120/bin/Hostx64/x64/cl.exe"
CMAKE_LINKER                 = "C:/Program Files/Microsoft Visual Studio/2022/Community/VC/Tools/MSVC/14.41.34120/bin/Hostx64/x64/link.exe"
CMAKE_INSTALL_PREFIX         = "D:/source/cenos/backend/temp/netgen"
CMAKE_CXX_COMPILER_LAUNCHER  = ""

version = NGSOLVE_VERSION_GIT

MKL_LINK = "sdl"

def get_cmake_dir():
    import os.path as p
    d_python = p.dirname(p.dirname(p.dirname(__file__)))
    py_to_cmake = p.relpath(
            NGSOLVE_INSTALL_DIR_CMAKE,
            NGSOLVE_INSTALL_DIR_PYTHON
            )
    return p.normpath(p.join(d_python,py_to_cmake))


def _resolve_install_dir(install_dir, python_dir, anchor_file):
    import os.path as p
    d_python = p.dirname(p.dirname(p.dirname(anchor_file)))
    return p.normpath(p.join(d_python, p.relpath(install_dir, python_dir)))


def get_include_dir():
    "Absolute path of the installed NGSolve headers."
    return _resolve_install_dir(
        NGSOLVE_INSTALL_DIR_INCLUDE, NGSOLVE_INSTALL_DIR_PYTHON, __file__)


def get_library_dir():
    "Absolute path of the installed NGSolve libraries."
    return _resolve_install_dir(
        NGSOLVE_INSTALL_DIR_LIB, NGSOLVE_INSTALL_DIR_PYTHON, __file__)


def get_netgen_include_dir():
    "Absolute path of the installed Netgen headers."
    import netgen.config as ngc
    return _resolve_install_dir(
        ngc.NG_INSTALL_DIR_INCLUDE, ngc.NG_INSTALL_DIR_PYTHON, ngc.__file__)


def get_netgen_library_dir():
    "Absolute path of the installed Netgen libraries."
    import netgen.config as ngc
    return _resolve_install_dir(
        ngc.NG_INSTALL_DIR_LIB, ngc.NG_INSTALL_DIR_PYTHON, ngc.__file__)


def get_include_dirs():
    "All include directories needed to compile against NGSolve (NGSolve and Netgen)."
    import os.path as p
    netgen_inc = get_netgen_include_dir()
    dirs = [get_include_dir(), netgen_inc, p.join(netgen_inc, "include")]
    unique = []
    for d in dirs:
        if d not in unique:
            unique.append(d)
    return unique


def get_library_dirs():
    "All library directories needed to link against NGSolve (NGSolve and Netgen)."
    dirs = [get_library_dir(), get_netgen_library_dir()]
    unique = []
    for d in dirs:
        if d not in unique:
            unique.append(d)
    return unique


def get_compile_include_args():
    "Compiler include flags (NGSolve + Netgen headers), formatted for the current platform."
    import sys
    if sys.platform == "win32":
        q = chr(34)
        return " ".join("/I" + q + d + q for d in get_include_dirs())
    return " ".join("-I" + d for d in get_include_dirs())


def get_link_lib_args():
    "Linker flags to link a shared library against NGSolve and Netgen, for the current platform."
    import sys
    dirs = get_library_dirs()
    if sys.platform == "win32":
        q = chr(34)
        args = ["/LIBPATH:" + q + d + q for d in dirs]
        args += ["nglib.lib", "ngcore.lib", "libngsolve.lib"]
        return " ".join(args)
    if sys.platform == "darwin":
        args = ["-L" + d for d in dirs]
        args.append("-undefined dynamic_lookup")
        return " ".join(args)
    args = []
    for d in dirs:
        args.append("-L" + d)
        args.append("-Wl,-rpath," + d)
    return " ".join(args)
